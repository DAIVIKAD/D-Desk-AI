# D Desk AI — Python Dependencies
# FastAPI Microservice Architecture v4.0.0

# ── Core Framework ──
fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.0

# ── Security & Authentication ──
bcrypt>=4.0.0

# ── ML / Classification ──
scikit-learn==1.6.1
numpy==2.2.4
pandas>=2.2.0

# ── LLM Integration ──
httpx>=0.27.0

# ── Cloud Database / Firebase ──
firebase-admin>=7.0.0

# ── Image Processing ──
pillow>=10.0.0
python-multipart>=0.0.9

# ── Image Classification (TensorFlow — optional) ──
# On Apple Silicon (M1/M2 Mac), install with:
#   pip install tensorflow-macos tensorflow-metal
# On Intel / Linux / GPU server, install with:
#   pip install tensorflow
# If TF is not installed the /api/image endpoint degrades gracefully.
